python


python中所有的数据类型 都是指针指向对应的数据所在的内存
js中只有引用类型(对象、数组)是类似指针指向对应的数据所在的内存，
        基本类型（null,string,number,boolean,undefine）不是


可变数据类型
  1.列表(list)
  2.字典(dict)（类似js 对象json）
在全局定义一个（变量）可变数据类型时 局部方法内改变全局定义的变量（可变数据类型）时，
全局的变量也跟着改变


不可变数据类型
   整型int、浮点型float、字符串型string和元组tuple
   在全局定义一个（变量）不可变数据类型时 局部方法内改变全局定义的变量（不可变数据类型）时，
全局的变量不会跟着改变，若要改变全局变量 要加global



创建对象（类 class）的过程
    1.创建一个对象
    2.python会自动调用_init_()方法  可以使用_init_方法添加熟悉
    3.返回创建的对象的引用


    4.类的私有方法  在方法名前 加__(两个下划线) 实例化对象调用不到这个私有方法

    5.__del__方法 只有引用计数==0时，__del__()才会被执行，并且定义了__del_()的实例无法被Python的循环垃圾收集器收集，
	所以尽量不要自定义__del__()。一般情况下，__del__() 不会破坏垃圾处理器

    6.继承、多态  https://www.cnblogs.com/feeland/p/4419121.html
